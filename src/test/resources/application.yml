app:
  jwt:
    secret: "this-is-a-very-strong-secret-key-for-testing-purposes-only-12345678"
    expirationMinutes: 120
  geo:
    base-url: https://nominatim.openstreetmap.org
    user-agent: "TravelMapCourse/1.0 (test)"
    default-limit: 8

logging:
  level:
    org.springframework.security: WARN

spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: Fy1976242
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  flyway:
    enabled: false

management:
  endpoints:
    web:
      exposure:
        include: ""